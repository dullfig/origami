{
  "name": "context-folding",
  "version": "1.0.0",
  "description": "Context folding - keeps full conversation history at variable resolution instead of lossy compaction",
  "hooks": "hooks/hooks.json",
  "skills": ["skills/context-folding"],
  "commands": ["commands"]
}
